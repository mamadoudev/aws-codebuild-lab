version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.12
    commands:
      - echo "Installation des dependances"
      - pip install -r requirements.txt

  build:
    commands:
      - echo "Execution des tests"
      - pytest